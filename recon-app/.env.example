# Dealio MT5 replica (direct from server, no tunnel needed)
DEALIO_HOST=cmtrading-replicadb.dealio.ai
DEALIO_PORT=5106
DEALIO_DB=dealio
DEALIO_USER=dealio_rep
DEALIO_PASS=;tnACp*BSIXZ2%mOHTfP

# MT5 datawarehouse (Postgres 109.199.112.72) — read-only role recon_reader.
# Authoritative MT5 deals/accounts source; replaces the Dealio replica.
DW_HOST=109.199.112.72
DW_PORT=5432
DW_DB=datawarehouse
DW_USER=recon_reader
DW_PASS=change-me

# Backoffice (reachable via Docker network on server)
BACKOFFICE_HOST=client-call-manager_postgres_1
BACKOFFICE_PORT=5432
BACKOFFICE_DB=backoffice
BACKOFFICE_USER=backoffice
BACKOFFICE_PASS=Hdtkfvi12345

FLASK_SECRET=change-me-in-prod

# Praxis operations DB — direct-access reporting replica (no SSH tunnel needed).
# Read-only reporting role; source of praxis_transactions for the /psps tab.
PRAXIS_HOST=13.140.163.221
PRAXIS_PORT=5434
PRAXIS_DB=operations
PRAXIS_USER=reporting_ro
PRAXIS_PASS=change-me
PRAXIS_SSLMODE=prefer

# PSPs tab (Basic Auth) — required or /psps returns HTTP 500
PSPS_USER=change-me
PSPS_PASS=change-me-in-prod

# Signals tab — CMTrading_Signals_EA ingestion (Bearer auth) + GUI (Basic Auth).
# Dedicated Postgres (signals_postgres container, docker-compose.yml) — own DB,
# not shared with any other domain.
SIGNALS_USER=SigManager
SIGNALS_PASS=change-me-in-prod
SIGNALS_API_KEY=change-me-in-prod
SIGNALS_PG_HOST=signals_postgres
SIGNALS_PG_PORT=5432
SIGNALS_PG_DB=signals
SIGNALS_PG_USER=signals
SIGNALS_PG_PASS=change-me-in-prod
